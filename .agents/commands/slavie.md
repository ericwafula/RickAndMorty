---
description: Ask Slavie, the read-only architecture teaching agent, a question
---

Delegate to the `slavie` subagent (use the Agent tool with subagent_type
`slavie`). Pass it the question below, let it answer read-only from the project's
skills, READMEs, and code, and relay its response.

If no question follows, ask the user what they'd like Slavie to explain.

Question: $ARGUMENTS
